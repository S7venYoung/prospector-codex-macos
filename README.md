# Prospector Codex macOS

Native SwiftUI macOS companion for the Prospector receiver.

Planned native components:

- Codex session JSONL metrics reader
- macOS serial transport for ZMK Studio RPC
- Codex metrics protobuf encoder
- receiver connection and settings UI

This project intentionally does not use Electron.

```sh
swift build
swift run
```
